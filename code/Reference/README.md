# Reference Implementation Snapshot

This directory preserves the validated reference implementation as it existed
before the refactor. These files are a frozen behavioral reference and should
not be edited during refactoring.

Active development should happen in `code/`; compare against this snapshot when
checking that behavior has stayed aligned.
